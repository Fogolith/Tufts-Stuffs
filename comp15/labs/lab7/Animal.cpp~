#include "Animal.h"

Animal::Animal()
{
    name="";
    for(int i=0; i<16; i++)
    {
	traits[i]=0;
    }
}

Animal::Animal(string a, int values[])
{
    name=a;
    for(int i=0; i<16; i++)
    {
	traits[i]=values[i];
    }
}
