#include "Animal.h"

Animal::Animal(string a, int values[])
{
    name=a;
    for(int i=0; i<16; i++)
    {
	traits[i]=values[i];
    }
}

string Animal::getName()
{
    return name;
}

int Animal::getTrait(int a)
{
    int temp=traits[a];
    return temp;
}
